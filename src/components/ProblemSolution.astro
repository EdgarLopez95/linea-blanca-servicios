---
---

<section class="section pas-section">
	<div class="container">
		<h2>¿Tu nevera dejó de enfriar o tu lavadora hace ruidos extraños?</h2>
		<p class="agitation-text">
			Sabemos que un daño inesperado detiene tu vida. La ropa sucia se acumula, los alimentos corren riesgo y tu tranquilidad se rompe. No dejes que una falla técnica te arruine la semana.
		</p>
		<div class="solution-card">
			<h3>Tenemos la solución experta que necesitas</h3>
			<p class="solution-text">
				Contamos con técnicos certificados y experiencia comprobada para resolver cualquier problema de línea blanca en tu hogar.
			</p>
			<ul class="benefits-list">
				<li>⚡ Atención Rápida</li>
				<li>🛡️ Garantía Real</li>
				<li>👨‍🔧 Técnicos Expertos</li>
			</ul>
		</div>
	</div>
</section>

<style>
	.pas-section {
		background-color: var(--bg-body);
		text-align: center;
	}

	.pas-section h2 {
		color: var(--text-main);
		font-weight: 700;
		font-size: 32px;
		line-height: 1.3;
		margin-bottom: 30px;
	}

	.agitation-text {
		color: var(--text-muted);
		font-size: 18px;
		line-height: 1.6;
		max-width: 700px;
		margin: 0 auto 50px;
	}

	.solution-card {
		background-color: var(--bg-card);
		border: 2px solid var(--color-accent);
		border-radius: 16px;
		padding: 40px 20px;
		box-shadow: 0 10px 30px var(--shadow-color);
		max-width: 800px;
		margin: 0 auto;
		text-align: center;
	}

	.solution-card h3 {
		color: var(--text-main);
		font-size: 28px;
		font-weight: 700;
		margin-bottom: 20px;
	}

	.solution-text {
		color: var(--text-muted);
		font-size: 18px;
		line-height: 1.6;
		margin-bottom: 30px;
	}

	.benefits-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-wrap: wrap;
		gap: 20px;
		justify-content: center;
	}

	.benefits-list li {
		display: flex;
		align-items: center;
		gap: 12px;
		font-size: 18px;
		font-weight: 600;
		color: var(--text-main);
	}

	@media (min-width: 768px) {
		.pas-section h2 {
			font-size: 42px;
		}
	}
</style>


