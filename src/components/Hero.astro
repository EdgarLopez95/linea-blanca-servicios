---
---

<section class="hero-section">
	<img 
		src="/linea-blanca/assets/hero-bg.jpg" 
		alt="Servicio técnico de línea blanca en Palmira" 
		width="1920" 
		height="1080" 
		fetchpriority="high" 
		class="hero-bg-image"
		aria-hidden="true"
	/>
	<div class="container hero-content-wrapper">
		<h1>
			Reparación Experta de <span class="highlight">Línea Blanca</span> en Palmira
		</h1>
		<p>Técnicos certificados a domicilio. Recupera la tranquilidad de tu hogar hoy mismo.</p>
		<a href="https://wa.me/573183180948" class="btn btn-primary" target="_blank" rel="noopener noreferrer">
			Cotizar reparación
			<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
				<path d="M5 12h14"/>
				<path d="M12 5l7 7-7 7"/>
			</svg>
		</a>
	</div>
</section>

<style>
	.hero-section {
		min-height: 500px;
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		color: white;
		text-align: center;
		padding: 60px 0 0;
		overflow: hidden;
	}

	.hero-bg-image {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		z-index: 0;
	}

	.hero-section::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			rgba(5, 36, 86, 0.85),
			rgba(5, 36, 86, 0.7)
		);
		z-index: 1;
	}

	.hero-content-wrapper {
		position: relative;
		z-index: 2;
	}

	.hero-content-wrapper {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 20px;
		text-align: center;
	}

	.hero-section h1 {
		font-size: 36px;
		font-weight: 700;
		line-height: 1.2;
		margin: 0;
	}

	.hero-section .highlight {
		color: var(--color-accent);
	}

	.hero-section p {
		font-size: 16px;
		line-height: 1.6;
		margin: 0;
		color: rgba(255, 255, 255, 0.9);
		max-width: 600px;
	}

	.btn-primary svg {
		width: 24px;
		height: 24px;
		flex-shrink: 0;
	}

	@media (min-width: 768px) {
		.hero-section {
			min-height: 600px;
			padding: 80px 0 0;
		}

		.hero-content-wrapper {
			gap: 24px;
		}

		.hero-section h1 {
			font-size: 64px;
		}

		.hero-section p {
			font-size: 20px;
		}
	}
</style>

